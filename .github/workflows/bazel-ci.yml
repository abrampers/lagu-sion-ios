name: bazel-build

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - '*'

jobs:
  test:
    if: "!contains(github.event.head_commit.message, '[ci skip]')"
    runs-on: macos-10.15
    steps:
    - uses: actions/checkout@v2
    - name: Select Xcode 11.5
      run: sudo xcode-select -s /Applications/Xcode_11.5.app
    - name: Cache Gems
      uses: actions/cache@v1
      env:
        cache-name: cache-fastlane-bundle-install
      with:
        path: ./vendor/
        key: ${{ runner.os }}-lagu-sion-ios-${{ env.cache-name }}
        restore-keys: |
          ${{ runner.os }}-lagu-sion-ios-${{ env.cache-name }}
    - name: Install Gems
      run: |
        bundle install --deployment
    - name: Install SSH Client 🔑
      uses: webfactory/ssh-agent@v0.2.0
      with:
        ssh-private-key: ${{ secrets.DEPLOY_KEY }}
    - name: Cache Tests
      uses: actions/cache@v1
      env:
        cache-name: cache-bazel-test
      with:
        path: /private/var/tmp/_bazel_runner
        key: ${{ runner.os }}-lagu-sion-ios-${{ env.cache-name }}
        restore-keys: |
          ${{ runner.os }}-lagu-sion-ios-${{ env.cache-name }}
    - name: Prepare Variables
      run: |
        export BUNDLE_VERSION=$GITHUB_RUN_NUMBER
        ./build-system/prepare-build.sh LaguSion
      env:
        MATCH_PASSWORD: ${{ secrets.MATCH_PASSWORD }}
        APP_VERSION: 0.1
        BUNDLE_ID: id.abram.ios.LaguSion
    - name: Run SongTests
      run: bazel test //SongTests
    - name: Run NetworkingTests
      run: bazel test //NetworkingTests
    - name: Run FavoritesTests
      run: bazel test //FavoritesTests
    - name: Run MainTests
      run: bazel test //MainTests
    - name: Run LaguSionTests
      run: bazel test //LaguSionTests
  build:
    if: "!contains(github.event.head_commit.message, '[ci skip]')"
    needs: test
    runs-on: macos-10.15
    steps:
    - uses: actions/checkout@v2
    - name: Select Xcode 11.5
      run: sudo xcode-select -s /Applications/Xcode_11.5.app
    - name: Cache Gems
      uses: actions/cache@v1
      env:
        cache-name: cache-fastlane-bundle-install
      with:
        path: ./vendor/
        key: ${{ runner.os }}-lagu-sion-ios-${{ env.cache-name }}
        restore-keys: |
          ${{ runner.os }}-lagu-sion-ios-${{ env.cache-name }}
    - name: Install Gems
      run: |
        bundle install --deployment
    - name: Install SSH Client 🔑
      uses: webfactory/ssh-agent@v0.2.0
      with:
        ssh-private-key: ${{ secrets.DEPLOY_KEY }}
    - name: Cache Build
      uses: actions/cache@v1
      env:
        cache-name: cache-bazel-build
      with:
        path: /private/var/tmp/_bazel_runner
        key: ${{ runner.os }}-lagu-sion-ios-${{ env.cache-name }}
        restore-keys: |
          ${{ runner.os }}-lagu-sion-ios-${{ env.cache-name }}
    - name: Prepare Variables
      run: |
        export BUNDLE_VERSION=$GITHUB_RUN_NUMBER
        ./build-system/prepare-build.sh LaguSion
      env:
        MATCH_PASSWORD: ${{ secrets.MATCH_PASSWORD }}
        APP_VERSION: 0.1
        BUNDLE_ID: id.abram.ios.LaguSion
    - name: Build iOS Application
      run: |
        bazel build //LaguSion --config=device_adhoc
    - name: Upload artifacts to AppCenter
      run: |
        bundle exec fastlane appcenter
      env:
        APPCENTER_API_TOKEN: ${{ secrets.APPCENTER_API_TOKEN }}
        APPCENTER_OWNER_NAME: "abram"
        APPCENTER_APP_NAME: "Lagu-Sion"
        SLACK_URL: ${{ secrets.SLACK_URL }}
        SLACK_CHANNEL: "#ios-build"
